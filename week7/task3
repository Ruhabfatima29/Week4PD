#include<iostream>
using namespace std;
main()
{
int number;
int count = 0;
cout << "Enter any number: ";
cin  >> number;
while(number%10 != 0)
{
if(number/10 != 0 )
count = count +1;
number = number/10; 
}
cout << count ;
}